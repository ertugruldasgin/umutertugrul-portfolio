import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = {
  title: "Uses — Umut Ertugrul",
};

export default function UsesPage() {
  return (
    <PagePlaceholder
      title="uses"
      note="hardware, software, and tools I use daily"
    />
  );
}
